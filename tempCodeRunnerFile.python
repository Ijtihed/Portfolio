class Solution {
    def maximumWealth(accounts: Array<IntArray>): Int {
        
        max_wealth = 0
    
        for customer_accounts in accounts:
            customer_wealth = sum(customer_accounts)
        
        if customer_wealth > max_wealth:
            max_wealth = customer_wealth
            
        return max_wealth
    }
}